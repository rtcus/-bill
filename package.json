{
  "name": "bill-management-system",
  "version": "1.0.0",
  "description": "一个功能完整的Web端账单管理系统，基于LeanCloud云存储",
  "main": "index.html",
  "scripts": {
    "build": "node build.js",
    "build:prod": "NODE_ENV=production node build.js",
    "build:dev": "NODE_ENV=development node build.js",
    "dev": "npm run build:dev && python -m http.server 8080 || python3 -m http.server 8080 || npx serve .",
    "start": "python -m http.server 8080 || python3 -m http.server 8080 || npx serve .",
    "deploy": "npm run build:prod && git add . && git commit -m 'Update configuration' && git push"
  },
  "keywords": [
    "bill-management",
    "leancloud",
    "web-app",
    "invoice",
    "accounting"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "dotenv": "^16.0.3"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/bill-management-system.git"
  },
  "bugs": {
    "url": "https://github.com/your-username/bill-management-system/issues"
  },
  "homepage": "https://github.com/your-username/bill-management-system#readme"
}